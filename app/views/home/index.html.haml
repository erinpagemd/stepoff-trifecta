= link_to "Walking Tracks", tracks_path

- if logged_in?
  = link_to "Sign Out", user_session_path, method: :delete
- else
  = link_to "Sign Up", new_user_path
  = link_to "Sign In", new_user_session_path
